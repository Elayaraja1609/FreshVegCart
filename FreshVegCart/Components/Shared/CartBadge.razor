@inject CartService CartService
@if (CartService.Count > 0)
{
    <span class="tabbar-badge bg-app-light text-white">
        @CartService.CountDispaly
    </span>    
}
@code {
    protected override void OnInitialized()
    {
        CartService.CartCountChange += StateHasChanged;
    }
    private void OnCartCountChanged() => InvokeAsync(StateHasChanged);
    public void Dispose()
    {
        CartService.CartCountChange -= StateHasChanged;
    }
}
<style>
    .tabbar-badge{
        height:20px;
        width:20px;
        position: absolute;
        top:-10px;
        right:-10px;
        line-height:20px;
        border-radius: 50%;
        font-size:10px;
        text-align:center;
    }

</style>